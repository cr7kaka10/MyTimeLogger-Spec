# Historical placeholder only. Do not copy this file to a runtime .env.
#
# Current configuration boundaries:
# - server/config.json: generated runtime/security/CORS settings
# - server/.env.example: optional, secret-free container runtime template
# - deploy/config.example.env: private deployment-target template
# - TickTick/S3/AI/aTimeLogger: configured per authenticated user
#
# Follow the repository root README.md for current Windows startup steps.
