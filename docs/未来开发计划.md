# MyTimeLogger 未来开发计划

> **最后更新**: 2026-04-12
> **当前基线**: PC 桌面端 (PyQt6)，倒计时专注 + TickTick 日清单同步 (持久连接 30s 轮询)，SQLite/MySQL 双数据库。

---

## 架构总览

```
┌─────────────────────────────────────────────────────────┐
│                    FastRhino R66S (OpenWrt)              │
│                Docker: FastAPI + MySQL                   │
│                WebSocket Hub 实时同步                     │
└────────────┬──────────────────────────┬──────────────────┘
             │ REST + WS               │ REST + WS
     ┌───────▼────────┐        ┌───────▼────────────┐
     │  PC 桌面端       │        │  安卓端              │
     │  PyQt6          │        │  Jetpack Compose    │
     │  - 倒计时专注    │        │  - 柳比歇夫计时      │
     │  - 柳比歇夫面板  │        │  - 时间书 Timeline   │
     │  - TickTick清单  │        │  - 桌面Widget闪念    │
     └────────────────┘        └─────────────────────┘
```

---

## 阶段 1（当前优先）：PC 端 — 柳比歇夫分类计时界面

> **策略**: 先做纯本地版本，不依赖服务端。后续阶段 2 部署服务端后再接入同步。

### 功能描述

仿 aTimeLogger Pro 的圆形图标网格界面，执行柳比歇夫时间管理法：

- **一级分类**: 输入（学习/阅读/编程...）、输出（写作/教学/创作...）、生活（运动/饮食/社交...）
- 每个分类有**自定义图标 + 颜色**
- 点击分类图标 → 调用已有的倒计时专注核心 (`logic.py` 的 `start_with_context()`)
- 计时结束后自动归类记录
- 本地 SQLite 存储分类和记录

### UI 草图

```
┌──────────────────────────────────┐
│  📊 柳比歇夫活动面板        ─ ×  │
│──────────────────────────────────│
│  ── 输入 ──                      │
│  ┌────┐  ┌────┐  ┌────┐        │
│  │ 📖 │  │ 💻 │  │ 🎧 │        │
│  │阅读 │  │编程 │  │听课 │       │
│  └────┘  └────┘  └────┘        │
│  ── 输出 ──                      │
│  ┌────┐  ┌────┐  ┌────┐        │
│  │ ✍️ │  │ 🎬 │  │ 📝 │        │
│  │写作 │  │教学 │  │笔记 │       │
│  └────┘  └────┘  └────┘        │
│  ── 生活 ──                      │
│  ┌────┐  ┌────┐  ┌────┐        │
│  │ 🏃 │  │ 🍽️ │  │ 💬 │        │
│  │运动 │  │饮食 │  │社交 │       │
│  └────┘  └────┘  └────┘        │
│──────────────────────────────────│
│  今日: 输入 4h32m  输出 2h10m    │
└──────────────────────────────────┘
```

### 需要新增/修改的文件

| 文件 | 动作 | 说明 |
|------|------|------|
| `category_manager.py` | 新增 | 分类 CRUD，本地 JSON/SQLite 存储 |
| `activity_panel.py` | 新增 | 柳比歇夫主面板（图标网格 + 分组 + 计时联动） |
| `logic.py` | 修改 | `start_with_context()` 增加 `category_id` 参数 |
| `gui.py` | 修改 | 右键菜单增加「活动面板」入口 |
| `database.py` | 修改 | `study_sessions` 表增加 `category_id` 字段 |

### 工时估算

| 子项 | 工时 | AI Token 消耗 |
|------|------|---------------|
| category_manager.py | 3h | ~30K |
| activity_panel.py (UI + 交互) | 6h | ~80K |
| logic.py / database.py 适配 | 3h | ~30K |
| 样式打磨 + 测试 | 3h | ~30K |
| **小计** | **~15h** | **~170K tokens** |

### 与 TickTick 日清单的关系

两套体系**并行不冲突**：
- **TickTick 日清单** → 管理「做什么」（任务清单）
- **柳比歇夫分类** → 管理「时间花在哪」（时间记录）

点击日清单的「专注」按钮时，可选择归属的柳比歇夫分类。

---

## 阶段 2：服务端 API + Docker 部署

> **前置条件**: 阶段 1 完成，PC 端柳比歇夫功能本地验证通过。

### 部署目标硬件（已确认）

| 参数 | 实况 |
|------|------|
| 设备 | **FastRhino R66S** |
| 架构 | **ARMv8 (aarch64)** 四核 1416MHz |
| RAM | **2GB** (当前可用 1.8GB / 90%) |
| 平均负载 | 0.03 ~ 0.12（极低） |
| 固件 | OpenWrt R22.10.10 |
| 内核 | 4.19.245 |

> **结论**: 2GB RAM + 四核 ARM，跑 FastAPI + MySQL 绰绰有余。关键是做好资源隔离。

### 资源隔离策略（不影响上网）

```yaml
# docker-compose.yml 关键配置
services:
  api:
    deploy:
      resources:
        limits:
          memory: 256M        # 硬限制 256MB
          cpus: '0.5'         # 最多用 0.5 个核心
    restart: unless-stopped

  db:
    image: mysql:8.0
    deploy:
      resources:
        limits:
          memory: 384M        # MySQL 硬限制 384MB
          cpus: '0.5'
    command: >
      --innodb-buffer-pool-size=64M
      --max-connections=20
      --performance-schema=OFF
    volumes:
      - /mnt/sda1/mysql_data:/var/lib/mysql   # 外接存储
```

**总内存预算**: 256MB (API) + 384MB (MySQL) = **640MB**，仅占总 RAM 的 32%。
**CPU 限制**: 两个容器各 0.5 核，合共 1 核，路由器剩余 3 核处理网络。

### 额外保障措施

1. **Docker 网络隔离**: 容器使用 `bridge` 网络，不干扰路由器 `br-lan`
2. **端口映射**: 只暴露 `8080:8000`（API），不占用常规端口
3. **OOM 保护**: Docker 有 OOM Score 调整，路由核心进程优先级更高
4. **外接存储**: MySQL 数据目录挂载到 USB/TF 卡，不写路由内置 Flash
5. **限速可选**: 可给 Docker bridge 网口做 QoS 限速

### 技术选型

| 组件 | 选型 | 理由 |
|------|------|------|
| Web 框架 | FastAPI (Python) | 异步原生、WebSocket 内建 |
| 数据库 | MySQL 8.0 (Docker) | 已有 MySQL 配置可复用 |
| ORM | SQLAlchemy 2.0 + alembic | 异步 + Migration |
| 实时通信 | WebSocket (FastAPI 原生) | 多端状态推送 |
| 认证 | JWT (python-jose) | 轻量统一 |

### 核心 API

```
POST   /api/auth/login           # JWT 登录
POST   /api/auth/refresh          # 刷新 token

GET    /api/categories            # 获取分类
POST   /api/categories            # 创建分类

POST   /api/sessions/start        # 开始计时
POST   /api/sessions/pause        # 暂停
POST   /api/sessions/stop         # 结束记录
GET    /api/sessions              # 查询记录
GET    /api/sessions/timeline     # 时间书格式

POST   /api/notes                 # 闪念笔记
GET    /api/notes                 # 查询笔记

WS     /ws/sync                   # WebSocket 实时同步
```

### 服务端文件结构

```
server/
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
├── alembic/
├── app/
│   ├── main.py
│   ├── config.py
│   ├── auth/
│   ├── models/
│   ├── schemas/
│   ├── routers/
│   │   ├── categories.py
│   │   ├── sessions.py
│   │   ├── notes.py
│   │   └── ws.py
│   ├── services/
│   └── db.py
└── nginx.conf (可选)
```

### Dockerfile 注意事项（ARM 构建）

```dockerfile
# 必须用 ARM 兼容的基础镜像
FROM python:3.11-slim-bookworm

# 在 PC 上交叉编译: docker buildx build --platform linux/arm64 -t mytimelogger-api .
# 然后 docker save | ssh load 到路由器
```

### 工时估算

| 子项 | 工时 | AI Token 消耗 |
|------|------|---------------|
| FastAPI 脚手架 + 数据模型 | 4h | ~50K |
| JWT 认证 | 2h | ~20K |
| Session / Category / Note API | 6h | ~60K |
| WebSocket 实时同步 | 4h | ~50K |
| Docker 化 + ARM 构建 + compose | 3h | ~25K |
| PC 端 sync_client.py 接入 | 4h | ~50K |
| 数据迁移脚本 (本地→服务端) | 2h | ~15K |
| **小计** | **~25h** | **~270K tokens** |

---

## 阶段 3：安卓端 MVP — 柳比歇夫计时界面

### 技术选型

| 组件 | 选型 |
|------|------|
| 语言 | Kotlin |
| UI | Jetpack Compose + Material3 |
| 网络 | Ktor Client (REST + WebSocket) |
| 本地缓存 | Room |
| DI | Hilt |
| 后台计时 | Foreground Service |
| 架构 | MVVM + Clean Architecture |

### 核心功能

- 与 PC 端一致的分类图标网格
- 点击图标开始计时（前台服务保活，通知栏常驻）
- 底部 Tab: 活动 | 时间书 | 统计
- 与服务端实时同步

### 文件结构

```
android/app/src/main/java/com/mytimelogger/
├── data/
│   ├── local/          # Room DAOs
│   ├── remote/         # Ktor API + WebSocket
│   └── repository/
├── domain/
│   ├── model/
│   └── usecase/
├── presentation/
│   ├── activity/       # 柳比歇夫主界面
│   ├── timeline/       # 时间书
│   └── theme/          # Material3 主题
├── service/
│   └── TimerService.kt
├── widget/
│   └── NoteWidget.kt
└── di/
```

### 工时估算

| 子项 | 工时 | AI Token 消耗 |
|------|------|---------------|
| 项目脚手架 + Hilt + 主题 | 4h | ~40K |
| Room 数据库 + 离线模型 | 4h | ~40K |
| Ktor 网络层 + JWT | 4h | ~50K |
| 柳比歇夫主界面 (Compose) | 6h | ~80K |
| TimerService 前台服务 | 4h | ~50K |
| WebSocket 实时同步 | 4h | ~50K |
| 样式/动画打磨 | 4h | ~40K |
| **小计** | **~30h** | **~350K tokens** |

---

## 阶段 4：安卓端 — 时间书 (Timeline)

### 功能描述

将专注记录、暂停记录、闪念笔记按**时间顺序**串联为竖向时间线：

```
┌─ 2026-04-12 ────────────────────┐
│                                  │
│  08:30 ── 📖 阅读《xxx》         │
│    │      25min                  │
│  08:55 ── ☕ 暂停 (喝水)         │
│    │      5min                   │
│  09:00 ── 💻 编程                │
│    │      45min                  │
│    │      "完成了登录模块"        │
│  09:45 ── 💬 闪念笔记            │
│    │      "下午要买菜"            │
│  10:00 ── 🏃 运动                │
│    │      30min                  │
│                                  │
│  ┌── 今日汇总 ────────────────┐  │
│  │ 输入 1h10m  输出 0h  生活 30m │
│  └────────────────────────────┘  │
└──────────────────────────────────┘
```

### 工时估算

| 子项 | 工时 | AI Token 消耗 |
|------|------|---------------|
| Timeline API + 混合排序 | 3h | ~30K |
| Compose Timeline UI | 6h | ~80K |
| 日历选择器 + 日期切换 | 2h | ~20K |
| 今日汇总卡片 | 2h | ~20K |
| **小计** | **~13h** | **~150K tokens** |

---

## 阶段 5：安卓端 — 桌面 Widget (闪念笔记)

### 功能描述

仿 flomo 的桌面小组件：

- **1×1 快捷入口**: 点击弹出极简输入框，一键记录闪念
- **2×4 回顾卡片**: 显示最近笔记，轮播回顾
- 笔记自动同步到服务端，出现在时间书中

### 技术要点

| 技术 | 说明 |
|------|------|
| GlanceAppWidget | Jetpack Glance (Compose 风格 Widget API) |
| WorkManager | 定时同步笔记 |
| RemoteInput | 通知栏直接输入 (Android 12+) |

### 工时估算

| 子项 | 工时 | AI Token 消耗 |
|------|------|---------------|
| Note API 客户端适配 | 2h | ~20K |
| 1×1 快捷录入 Widget | 3h | ~30K |
| 2×4 回顾卡片 Widget | 4h | ~40K |
| 输入弹窗 + 同步逻辑 | 3h | ~30K |
| **小计** | **~12h** | **~120K tokens** |

---

## 阶段 6：多端实时同步 + 数据一致性

### 同步流程

```
PC 端 ──start──▶ WebSocket Hub ──推送──▶ 安卓端 (UI 更新)
安卓端 ──stop──▶ WebSocket Hub ──推送──▶ PC 端 (缓存刷新)
```

### 离线策略

- 断网时本地 Room/SQLite 暂存事件队列
- 联网后自动推送，冲突解决: Last-Write-Wins + 事件版本号
- 启动时先全量同步一次

### 工时估算

| 子项 | 工时 | AI Token 消耗 |
|------|------|---------------|
| WebSocket 事件协议设计 | 2h | ~20K |
| 服务端 Hub 广播 + 房间管理 | 4h | ~50K |
| PC 端接入 | 3h | ~30K |
| 安卓端接入 | 3h | ~30K |
| 离线队列 + 冲突解决 | 4h | ~50K |
| 端到端测试 | 3h | ~30K |
| **小计** | **~19h** | **~210K tokens** |

---

## 总体资源汇总

### 工时 & Token

| 阶段 | 描述 | 工时 | AI Token | AI 请求数 |
|------|------|------|----------|----------|
| 1 | PC 端柳比歇夫界面 (纯本地) | 15h | ~170K | ~45 |
| 2 | 服务端 API + Docker (R66S) | 25h | ~270K | ~70 |
| 3 | 安卓端 MVP (计时) | 30h | ~350K | ~90 |
| 4 | 安卓端 时间书 | 13h | ~150K | ~40 |
| 5 | 安卓端 桌面 Widget | 12h | ~120K | ~30 |
| 6 | 多端实时同步 | 19h | ~210K | ~55 |
| | **总计** | **~114h** | **~1.27M** | **~330** |

> 工时按 AI 辅助编程估算（纯手写约 2.5~3x）

### 部署资源 (OpenWrt R66S)

| 资源 | 用量 | 占比 |
|------|------|------|
| RAM (容器总计) | 640MB | 32% of 2GB |
| CPU (容器限制) | 1 核 | 25% of 4 核 |
| 存储 (镜像+数据) | ~2GB | 外接 TF/USB |
| 网络端口 | 8080 | 不影响 80/443 |

### 推荐执行顺序

```
阶段 1 ──▶ 阶段 2 ──▶ 阶段 3 ──▶ 阶段 4 ──▶ 阶段 5 ──▶ 阶段 6
PC本地     服务端      安卓MVP     时间书      Widget     联调
~15h       ~25h       ~30h       ~13h       ~12h       ~19h
```

**里程碑拆分建议**:
- **M1**: 阶段 1 — PC 端柳比歇夫界面跑通，纯本地验证
- **M2**: 阶段 2 — 服务端部署到 R66S，PC 端接入同步
- **M3**: 阶段 3~6 — 安卓端全部功能 + 多端联调

---

## 风险与注意事项

| 风险 | 缓解措施 |
|------|---------|
| R66S 性能不够 | Docker 资源限制已设 (640MB / 1核)，留足余量给路由 |
| Docker 影响上网 | bridge 网络隔离 + CPU/MEM 硬限制 + 不占用常规端口 |
| ARM 镜像构建慢 | PC 上 `docker buildx` 交叉编译，`docker save/load` 传到路由 |
| 安卓后台计时被杀 | Foreground Service + 通知栏 + 引导用户加电池白名单 |
| WebSocket 断线 | 指数退避重连 + 离线事件队列 + 启动全量同步 |
| 外网访问需求 | DDNS + 端口转发 或 frp/Tailscale 内网穿透 |
