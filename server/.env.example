# Optional Docker Compose overrides. Copy to server/.env only when needed.
# User provider credentials belong in the authenticated server configuration UI.
MYTIMELOGGER_ENVIRONMENT=development
SLEEP_SERVER_PORT=8000
